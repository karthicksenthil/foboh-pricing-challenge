{
  "name": "foboh-pricing-challenge",
  "version": "1.0.0",
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:backend": "cd backend && npm install && npm run build",
    "copy:frontend": "rimraf backend/dist/dist && mkdirp backend/dist/dist && copyfiles -u 1 frontend/dist/**/* backend/dist",
    "start": "cd backend && node dist/index.js",
    "deploy": "npm-run-all build:frontend  build:backend copy:frontend start"
    
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "mkdirp": "^1.0.4",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  }
}